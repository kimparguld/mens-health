// Vitest stub for the "server-only" Next.js package.
// The real package throws at import time to prevent server code from being
// bundled into client bundles. In the Vitest environment there is no such
// concern, so this stub is a no-op.
export {};
